{# staging → target 적재 INSERT. 날짜 독립(모든 task 공유). append 적재이므로, 재실행 멱등이
   필요하면 대상 테이블을 job 밖에서 미리 비우거나(TRUNCATE 등) 날짜별 물리 테이블을 쓴다.
   테이블명은 따옴표 없이 bare 로 렌더한다. #}
INSERT INTO {{ target_table }} (user_id, amount, region, dt)
SELECT user_id, amount, region, dt
FROM {{ staging_table }}
