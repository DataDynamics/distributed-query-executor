# Coordinator 런타임 의존성 (RHEL 9.2 기본 Python 3.9+ 기준)
# 0.129+ 는 Python 3.10+ 를 요구하므로 3.9 호환을 위해 0.128.x 로 상한을 둔다.
fastapi>=0.110,<0.129
# 0.40+ 는 Python 3.10+ 를 요구하므로 3.9 호환을 위해 0.39.x 로 상한을 둔다.
uvicorn[standard]>=0.29,<0.40
sqlglot>=30,<31
httpx>=0.28,<1.0
pydantic>=2.13,<3.0
PyYAML>=6.0
# 쿼리 템플릿 엔진(SELECT/STAGING DDL/INSERT 를 서버 템플릿 파일로 런타임 생성).
# 3.1.x 는 Python 3.9+ 호환. 에어갭 환경에선 오프라인 wheel 로 설치한다.
Jinja2>=3.1,<4.0
# /metrics 의 CPU/메모리/디스크 수집
psutil>=5.9
# coordinator 의 executor 메트릭을 PostgreSQL 에 기록(monitor.db_dsn 설정 시)
psycopg[binary]>=3.2
